# キーアート画像の置き場所

`keyart.png` をここに置くと、タイトル画面の背景として自動的に表示されます
（`index.html` の `#keyartLayer` が `art/keyart.png` を読みに行きます）。

- ファイルが無い、または読み込めない場合は自動的に非表示になるだけなので、
  置かなくてもゲームは問題なく動作します。
- 推奨: 縦長（9:16 程度）、暗めの配色（タイトル文字が読めるよう上部を暗く）。
