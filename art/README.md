# キーアート画像

`keyart.jpg` がタイトル画面の背景として自動的に表示されます
（`index.html` の `#keyartLayer` が `art/keyart.jpg` を読みに行きます）。

- ファイルが無い、または読み込めない場合は自動的に非表示になるだけなので、
  無くてもゲームは問題なく動作します。
- 差し替える場合の推奨: 縦長（9:16 程度）、暗めの配色（タイトル文字が読めるよう上部を暗く）。
- 容量を抑えるため JPEG（quality 85 前後）を使っています。透過が必要な絵柄でなければ
  PNGよりかなり軽くなります。
